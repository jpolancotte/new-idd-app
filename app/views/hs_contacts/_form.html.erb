<%= form_with(model: hs_contact) do |form| %>
  <% if hs_contact.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hs_contact.errors.count, "error") %> prohibited this hs_contact from being saved:</h2>

      <ul>
        <% hs_contact.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
